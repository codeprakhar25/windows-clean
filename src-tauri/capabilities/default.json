{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability for the read-only SpaceGuard shell. No filesystem, shell, registry, or updater plugin permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
